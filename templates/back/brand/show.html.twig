{% extends 'back/base.html.twig' %}

{% block title %} {% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_brand_index') }}" class="btn btn-dark">back to list</a>

    <a href="{{ path('app_back_brand_edit', {'id': brand.id}) }}" class="btn btn-dark">edit</a>

    {{ include('back/brand/_delete_form.html.twig') }}
{% endblock %}
