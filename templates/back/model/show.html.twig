{% extends 'back/base.html.twig' %}

{% block title %}Détail du model{% endblock %}

{% block body %}

    <div class="container">
        <h1>Model</h1>
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ model.description }}</td>
            </tr>
            </tbody>
        </table>
        <div class="container">
            <div class="row" >
                <a href="{{ path('app_back_model_index') }}" class="btn btn-dark col-2">back to list</a>

                <a href="{{ path('app_back_model_edit', {'id': model.id}) }}" class="btn btn-dark col-2 ">edit</a>

                {{ include('back/model/_delete_form.html.twig') }}
            </div>

        </div>


    </div>






{% endblock %}
