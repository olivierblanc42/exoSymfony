{% extends 'front/base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="d-flex justify-content-between">
            <h2  class="mt-3 mb-4">Nos Meilleurs<span class="span-green"> Produits </span></h2>
        </div>
        <div class="row  ">
            {% for productIteration in listing %}
                <div class="col-lg-4 col-md-12">
                    {% include 'front/partials/listingCard.html.twig' with {
                        'listing': productIteration
                    } %}
                </div>
            {% endfor %}
        </div>
        <div class="navigation d-flex justify-content-center mt-2">
            {{ knp_pagination_render(listing) }}
        </div>
    </div>
{% endblock %}
